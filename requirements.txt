# Python dependencies for this project (single source at repo root).
# Install: pip install -r requirements.txt
# Frontend uses frontend/package.json (npm install) — not listed here.

# Runtime
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
psycopg2-binary>=2.9.10
alembic>=1.14.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-multipart>=0.0.17
pyyaml>=6.0.2
httpx>=0.28.0

# Dev / test
pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.8.0

# Optional local STT (Docker speech path)
faster-whisper>=1.1.0
