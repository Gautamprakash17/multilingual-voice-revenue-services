# Integration / repo-level tests

Backend unit and invariant tests live in `backend/tests/`.

This folder is reserved for future cross-service smoke checks (e.g. Compose-level).
P1 keeps critical tests next to the FastAPI app for fast local runs.
