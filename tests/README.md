# Integration / repo-level tests

Backend unit and invariant tests live in `backend/tests/`.

This folder is reserved for future cross-service smoke checks (for example, Compose-level checks).
Critical tests remain next to the FastAPI app for fast local runs.
